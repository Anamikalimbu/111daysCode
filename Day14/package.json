{
  "name": "day14",
  "version": "1.0.0",
  "description": "Components are the fundamental building blocks of a React application. They let you split the UI into independent, reusable pieces, and think about each piece in isolation.",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "react-router-dom": "^7.14.2"
  }
}
